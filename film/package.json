{
  "name": "storefront-desk-film",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Storefront Desk automated demo video production pipeline: multi-tier TTS narration generation (Kokoro/Gemini/espeak), Playwright 1080p plate capture, beat-matched FFmpeg assembly, and automated compliance verification.",
  "scripts": {
    "plates": "node --env-file-if-exists=../.env.local --import tsx record-plates.ts",
    "narration": "node --env-file-if-exists=../.env.local --import tsx generate-narration.ts",
    "assemble": "node --env-file-if-exists=../.env.local --import tsx assemble-reel.ts",
    "verify": "node --env-file-if-exists=../.env.local --import tsx verify-reel.ts",
    "build-film": "npm run assemble && npm run verify"
  }
}
